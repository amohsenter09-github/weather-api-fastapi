server:
  insecure: true   # serve HTTP (simplest for local ingress)

  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.local
    paths:
      - /
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "false"