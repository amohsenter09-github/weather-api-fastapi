apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

# All namespaced resources from the base will be deployed into this namespace
namespace: weather-api

