apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: weather-api
  labels:
    app: weather-api
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: weather-api
subjects:
  - kind: ServiceAccount
    name: weather-api
    namespace: weather-api  # Patched per overlay
